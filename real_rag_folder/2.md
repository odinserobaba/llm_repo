
## Пример 1: Навигационное меню

```html
<nav id="main-nav" class="navigation primary">
  <ul class="menu">
    <li class="menu-item active"><a href="/">Главная</a></li>
    <li class="menu-item"><a href="/about" data-section="info">О нас</a></li>
    <li class="menu-item"><a href="/services" title="Услуги компании">Услуги</a></li>
    <li class="menu-item"><a href="/contact">Контакты</a></li>
  </ul>
</nav>
```

**CSS селекторы:**
```css
/* 1. По тегу */
nav { background: #333; }
ul { padding: 0; }

/* 2. По классу */
.navigation { font-family: Arial; }
.menu-item { display: inline-block; }

/* 3. По ID */
#main-nav { height: 60px; }

/* 4. По вложенности */
nav ul li a { color: white; }

/* 5. Прямой дочерний элемент */
.navigation > ul { margin: 0; }

/* 6. По нескольким классам */
.navigation.primary { border-bottom: 2px solid blue; }

/* 7. По атрибуту */
a[href="/about"] { font-weight: bold; }

/* 8. По атрибуту со значением */
a[data-section="info"] { color: #ff9900; }

/* 9. По частичному совпадению атрибута */
a[href^="/"] { text-decoration: none; }

/* 10. Псевдокласс */
.menu-item:hover { background: #555; }
.active { background: #007bff; }

/* 11. Комбинатор соседнего элемента */
.active + .menu-item { margin-left: 10px; }

/* 12. Комбинатор последующих элементов */
.active ~ .menu-item { opacity: 0.9; }
```

## Пример 2: Карточка товара

```html
<article class="product-card featured" data-category="electronics" id="product-123">
  <div class="card-header">
    <h2 class="product-title">Смартфон XYZ</h2>
    <span class="badge new">Новинка</span>
    <span class="badge discount">-20%</span>
  </div>
  <img src="phone.jpg" alt="Смартфон XYZ" class="product-image main">
  <p class="description">Мощный смартфон с отличной камерой</p>
  <div class="price-container">
    <span class="price old">29990₽</span>
    <span class="price current">23990₽</span>
  </div>
  <button class="btn buy-btn" data-action="add-to-cart">Купить</button>
</article>
```

**CSS селекторы:**
```css
/* 1. Универсальный селектор */
* { box-sizing: border-box; }

/* 2. Селектор по тегу с классом */
article.product-card { border: 1px solid #ddd; }

/* 3. Селектор по тегу и атрибуту */
article[data-category] { border-left: 4px solid green; }

/* 4. Селектор потомков */
.product-card .price { font-size: 1.2em; }

/* 5. Дочерний селектор */
.card-header > .badge { display: inline-block; }

/* 6. Селектор по первому дочернему элементу */
.card-header > :first-child { font-size: 1.5em; }

/* 7. Селектор по последнему дочернему элементу */
.price-container > :last-child { color: red; }

/* 8. Селектор n-го дочернего элемента */
.card-header > :nth-child(2) { background: yellow; }

/* 9. Селектор каждого второго элемента */
.badge:nth-child(even) { margin-left: 5px; }

/* 10. Селектор по атрибуту с началом значения */
button[data-action^="add"] { background: green; }

/* 11. Селектор по атрибуту с окончанием значения */
button[class$="-btn"] { padding: 10px 20px; }

/* 12. Селектор по атрибуту с содержанием значения */
[class*="product"] { margin-bottom: 15px; }

/* 13. Селектор пустых элементов */
.description:empty { display: none; }

/* 14. Псевдоэлемент */
.product-title::before { content: "★ "; }
```

## Пример 3: Таблица данных

```html
<table class="data-table" id="users-table">
  <thead>
    <tr class="header-row">
      <th scope="col">ID</th>
      <th scope="col" class="sortable">Имя</th>
      <th scope="col">Email</th>
      <th scope="col">Статус</th>
    </tr>
  </thead>
  <tbody>
    <tr class="user-row active" data-user-id="1">
      <td>1</td>
      <td class="name">Анна Иванова</td>
      <td><a href="mailto:anna@example.com">anna@example.com</a></td>
      <td class="status active">Активен</td>
    </tr>
    <tr class="user-row inactive" data-user-id="2">
      <td>2</td>
      <td class="name">Петр Сидоров</td>
      <td><a href="mailto:petr@example.com">petr@example.com</a></td>
      <td class="status inactive">Неактивен</td>
    </tr>
  </tbody>
</table>
```

**CSS селекторы:**
```css
/* 1. Селектор по тегу и классу */
table.data-table { width: 100%; }

/* 2. Селектор по тегу вложенный */
tbody tr { border-bottom: 1px solid #eee; }

/* 3. Селектор по нескольким классам */
.user-row.active { background: #e8f5e9; }

/* 4. Селектор по атрибуту */
[data-user-id] { cursor: pointer; }

/* 5. Селектор по точному значению атрибута */
[data-user-id="1"] { font-weight: bold; }

/* 6. Структурный псевдокласс - первый потомок */
tbody tr:first-child { border-top: 2px solid #333; }

/* 7. Структурный псевдокласс - последний потомок */
tbody tr:last-child { border-bottom: none; }

/* 8. Структурный псевдокласс - нечетные строки */
tbody tr:nth-child(odd) { background: #f9f9f9; }

/* 9. Структурный псевдокласс - четные строки */
tbody tr:nth-child(even) { background: #fff; }

/* 10. Селектор по типу input (для ссылок как псевдо-инпут) */
a[href^="mailto:"] { color: #0066cc; }

/* 11. Селектор соседнего элемента */
.status.active + td { /* нет следующего td */ }

/* 12. Селектор последующих элементов */
.header-row ~ tr { font-size: 0.9em; }

/* 13. Селектор с отрицанием */
.user-row:not(.active) { opacity: 0.7; }

/* 14. Селектор по языку (гипотетически) */
:lang(ru) { font-family: 'Arial', sans-serif; }
```

## Пример 4: Форма ввода

```html
<form class="contact-form" id="signup-form" novalidate>
  <div class="form-group">
    <label for="username" class="required">Имя пользователя:</label>
    <input type="text" id="username" name="username" required 
           placeholder="Введите имя" minlength="3" class="form-input">
    <span class="hint">Минимум 3 символа</span>
  </div>
  
  <div class="form-group">
    <label for="email">Email:</label>
    <input type="email" id="email" name="email" 
           placeholder="email@example.com" class="form-input">
  </div>
  
  <div class="form-group">
    <label for="newsletter">Подписаться на рассылку:</label>
    <input type="checkbox" id="newsletter" name="newsletter" checked>
  </div>
  
  <div class="form-group">
    <label>Возраст:</label>
    <input type="radio" id="age1" name="age" value="18-25">
    <label for="age1">18-25</label>
    <input type="radio" id="age2" name="age" value="26-35">
    <label for="age2">26-35</label>
  </div>
  
  <button type="submit" class="btn btn-primary">Отправить</button>
  <button type="reset" class="btn btn-secondary">Сбросить</button>
</form>
```

**CSS селекторы:**
```css
/* 1. Селектор по тегу и атрибуту */
form[novalidate] { border: 1px solid #ccc; }

/* 2. Селектор по тегу и псевдоклассу */
input:required { border-left: 3px solid #ff6b6b; }

/* 3. Селектор по атрибуту */
input[type="text"] { width: 200px; }
input[type="email"] { width: 250px; }
input[type="checkbox"] { margin-right: 5px; }

/* 4. Селектор с состоянием */
input:focus { outline: 2px solid #4d90fe; }
input:disabled { background: #f5f5f5; }
input:checked + label { font-weight: bold; }

/* 5. Селектор по плейсхолдеру */
::placeholder { color: #999; }

/* 6. Селектор валидного поля */
input:valid { border-color: #4CAF50; }

/* 7. Селектор невалидного поля */
input:invalid { border-color: #f44336; }

/* 8. Селектор в пределах диапазона (для number) */
input[type="number"]:out-of-range { color: red; }

/* 9. Селектор по атрибуту с окончанием */
button[type$="mit"] { background: #4CAF50; }
button[type$="set"] { background: #9E9E9E; }

/* 10. Селектор потомка с определенным классом */
.form-group .form-input { padding: 8px; }

/* 11. Селектор общего соседа */
label ~ input { margin-top: 5px; }

/* 12. Селектор непосредственного соседа */
input + .hint { font-size: 0.8em; color: #666; }

/* 13. Селектор с псевдоэлементом */
label.required::after { content: " *"; color: red; }

/* 14. Селектор по месту в группе */
.form-group:first-of-type { margin-top: 0; }
.form-group:last-of-type { margin-bottom: 0; }
```

## Пример 5: Список комментариев

```html
<div class="comments-section">
  <h3 class="section-title">Комментарии <span class="count">(5)</span></h3>
  
  <ul class="comments-list">
    <li class="comment" data-author="admin" data-comment-id="101">
      <div class="comment-header">
        <img src="avatar1.jpg" alt="Аватар" class="avatar">
        <span class="author">Администратор</span>
        <time datetime="2024-01-15">15 января</time>
      </div>
      <p class="comment-text">Отличная статья, спасибо!</p>
      <button class="reply-btn">Ответить</button>
    </li>
    
    <li class="comment new" data-author="user123" data-comment-id="102">
      <div class="comment-header">
        <img src="avatar2.jpg" alt="Аватар" class="avatar">
        <span class="author">Иван Петров</span>
        <time datetime="2024-01-16">16 января</time>
      </div>
      <p class="comment-text">Есть вопросы по третьему пункту...</p>
      <button class="reply-btn">Ответить</button>
    </li>
    
    <li class="comment reply" data-author="admin" data-comment-id="103">
      <div class="comment-header">
        <span class="author">Администратор</span>
        <time datetime="2024-01-16">16 января</time>
      </div>
      <p class="comment-text">Что именно вас интересует?</p>
    </li>
  </ul>
</div>
```

**CSS селекторы:**
```css
/* 1. Селектор потомков */
.comments-section .comment { margin-bottom: 20px; }

/* 2. Селектор дочерних элементов */
.comments-list > li { border-left: 3px solid #007bff; }

/* 3. Селектор по атрибуту */
[data-author="admin"] { background: #f0f8ff; }

/* 4. Селектор по нескольким атрибутам */
.comment[data-author="admin"][data-comment-id] { border-left-color: #ff6b6b; }

/* 5. Селектор по части атрибута */
[data-comment-id^="10"] { padding: 10px; }

/* 6. Селектор пустых элементов */
.comment-text:empty { display: none; }

/* 7. Структурные псевдоклассы */
.comment:first-child { margin-top: 10px; }
.comment:last-child { margin-bottom: 0; }
.comment:nth-child(2) { background: #ffffe0; }

/* 8. Селектор типа элемента */
li.comment { list-style-type: none; }

/* 9. Селектор с отрицанием */
.comment:not(.reply) { margin-left: 0; }
.comment:not([data-author="admin"]) { border-left-color: #ccc; }

/* 10. Селектор соседних элементов */
.new + .comment { border-top: 1px dashed #ddd; }

/* 11. Селектор последующих элементов */
.new ~ .comment { opacity: 0.95; }

/* 12. Селектор по тегу внутри класса */
.comment-header > img.avatar { border-radius: 50%; }

/* 13. Селектор по месту среди элементов одного типа */
.comment:first-of-type { border-top-left-radius: 5px; }
.comment:last-of-type { border-bottom-left-radius: 5px; }

/* 14. Селектор единственного элемента среди родителя */
.comment:only-child { border: 2px solid #4CAF50; }

/* 15. Селектор псевдоэлемента */
.section-title::after { content: ""; display: block; height: 2px; background: #333; }
```

## Пример 6: Сетка изображений

```html
<div class="gallery" id="photo-gallery" data-gallery-version="2">
  <figure class="photo-item landscape featured">
    <img src="photo1.jpg" alt="Горный пейзаж" class="gallery-img">
    <figcaption class="caption">Горы на рассвете</figcaption>
  </figure>
  
  <figure class="photo-item portrait">
    <img src="photo2.jpg" alt="Портрет" class="gallery-img">
    <figcaption class="caption">Улыбающийся человек</figcaption>
  </figure>
  
  <figure class="photo-item landscape">
    <img src="photo3.jpg" alt="Город" class="gallery-img loading">
    <figcaption class="caption">Ночной город</figcaption>
  </figure>
  
  <div class="gallery-controls">
    <button class="control-btn prev" aria-label="Предыдущее фото">←</button>
    <button class="control-btn next" aria-label="Следующее фото">→</button>
    <button class="control-btn play" aria-label="Слайдшоу">▶</button>
  </div>
</div>
```

**CSS селекторы:**
```css
/* 1. Селектор по ID */
#photo-gallery { display: grid; }

/* 2. Селектор по тегу и классу */
figure.photo-item { margin: 10px; }

/* 3. Селектор по атрибуту */
[data-gallery-version] { background: #f5f5f5; }

/* 4. Селектор по значению атрибута */
[data-gallery-version="2"] { grid-template-columns: repeat(3, 1fr); }

/* 5. Селектор по классу и атрибуту */
.photo-item[class*="landscape"] { grid-column: span 2; }

/* 6. Селектор потомков */
.gallery .caption { font-style: italic; }

/* 7. Селектор дочерних элементов */
.gallery > figure { position: relative; }

/* 8. Селектор соседнего элемента */
.featured + .photo-item { border: 2px solid gold; }

/* 9. Селектор последующих элементов */
.featured ~ .photo-item { opacity: 0.9; }

/* 10. Селектор по атрибуту с началом */
button[aria-label^="Предыдущее"] { left: 10px; }
button[aria-label^="Следующее"] { right: 10px; }

/* 11. Селектор по атрибуту с содержанием */
button[aria-label*="фото"] { background: rgba(0,0,0,0.5); }

/* 12. Селектор по последнему дочернему элементу */
.gallery-controls > :last-child { background: #4CAF50; }

/* 13. Селектор по n-му дочернему элементу */
.gallery-controls > :nth-child(2) { background: #2196F3; }

/* 14. Селектор каждого третьего элемента */
.photo-item:nth-child(3n) { margin-right: 0; }

/* 15. Селектор каждого элемента начиная с 2-го */
.photo-item:nth-child(n+2) { margin-top: 20px; }

/* 16. Селектор с состоянием */
button:hover { opacity: 0.8; }
button:active { transform: scale(0.95); }
button:disabled { opacity: 0.5; }

/* 17. Селектор псевдоэлемента */
.caption::before { content: "📷 "; }
```

## Пример 7: Аккордеон/раскрывающийся блок

```html
<div class="accordion" id="faq-accordion">
  <section class="accordion-item open" data-category="general">
    <h4 class="accordion-header">
      <button class="accordion-button" aria-expanded="true">
        Как начать пользоваться сервисом?
      </button>
    </h4>
    <div class="accordion-content">
      <p>Просто зарегистрируйтесь на нашем сайте...</p>
    </div>
  </section>
  
  <section class="accordion-item" data-category="billing">
    <h4 class="accordion-header">
      <button class="accordion-button" aria-expanded="false">
        Какие способы оплаты вы принимаете?
      </button>
    </h4>
    <div class="accordion-content" hidden>
      <p>Мы принимаем карты Visa, MasterCard...</p>
    </div>
  </section>
  
  <section class="accordion-item" data-category="technical">
    <h4 class="accordion-header">
      <button class="accordion-button" aria-expanded="false">
        Технические требования
      </button>
    </h4>
    <div class="accordion-content" hidden>
      <p>Современный браузер и доступ в интернет...</p>
    </div>
  </section>
</div>
```

**CSS селекторы:**
```css
/* 1. Селектор по тегу и классу */
section.accordion-item { border: 1px solid #ddd; }

/* 2. Селектор по атрибуту */
[data-category] { border-left-width: 4px; }

/* 3. Селектор по значению атрибута */
[data-category="general"] { border-left-color: #4CAF50; }
[data-category="billing"] { border-left-color: #FF9800; }
[data-category="technical"] { border-left-color: #2196F3; }

/* 4. Селектор с состоянием */
.accordion-button[aria-expanded="true"] { background: #e3f2fd; }
.accordion-button[aria-expanded="false"] { background: #f5f5f5; }

/* 5. Селектор по атрибуту */
[hidden] { display: none; }

/* 6. Селектор открытого элемента */
.accordion-item.open .accordion-content { display: block; }

/* 7. Селектор соседнего элемента */
.accordion-item.open + .accordion-item { margin-top: 5px; }

/* 8. Селектор дочерних элементов */
.accordion-item > .accordion-header { margin: 0; }

/* 9. Селектор потомков */
.accordion .accordion-content { padding: 15px; }

/* 10. Селектор с псевдоклассом состояния */
.accordion-button:hover { background: #e0e0e0; }
.accordion-button:focus { outline: 2px solid #4d90fe; }

/* 11. Селектор с псевдоэлементом */
.accordion-button::after { 
  content: "▼"; 
  float: right; 
}

.accordion-button[aria-expanded="true"]::after { 
  content: "▲"; 
}

/* 12. Селектор по первому дочернему элементу */
.accordion-item:first-child { border-top-left-radius: 5px; }

/* 13. Селектор по последнему дочернему элементу */
.accordion-item:last-child { border-bottom-left-radius: 5px; }

/* 14. Селектор каждого второго элемента */
.accordion-item:nth-child(2n) { background: #fafafa; }

/* 15. Селектор всех кроме первого */
.accordion-item:not(:first-child) { border-top: none; }

/* 16. Селектор по месту среди элементов одного типа */
.accordion-item:first-of-type { border-top: 2px solid #333; }
```

## Пример 8: Хлебные крошки

```html
<nav aria-label="Хлебные крошки" class="breadcrumbs">
  <ol class="breadcrumbs-list">
    <li class="breadcrumb-item">
      <a href="/" class="breadcrumb-link">Главная</a>
    </li>
    <li class="breadcrumb-item">
      <a href="/catalog" class="breadcrumb-link">Каталог</a>
    </li>
    <li class="breadcrumb-item">
      <a href="/catalog/electronics" class="breadcrumb-link">Электроника</a>
    </li>
    <li class="breadcrumb-item current" aria-current="page">
      <span class="breadcrumb-text">Смартфоны</span>
    </li>
  </ol>
</nav>
```

**CSS селекторы:**
```css
/* 1. Селектор по тегу и атрибуту */
nav[aria-label] { font-size: 0.9em; }

/* 2. Селектор по тегу и классу */
ol.breadcrumbs-list { display: flex; }

/* 3. Селектор потомков */
.breadcrumbs .breadcrumb-item { margin-right: 5px; }

/* 4. Селектор дочерних элементов */
.breadcrumbs-list > li { list-style-type: none; }

/* 5. Селектор по классу и атрибуту */
.breadcrumb-item[aria-current] { font-weight: bold; }

/* 6. Селектор по атрибуту */
[aria-current="page"] { color: #333; }

/* 7. Селектор всех кроме последнего */
.breadcrumb-item:not(:last-child)::after { 
  content: "›"; 
  margin-left: 5px; 
}

/* 8. Селектор всех кроме первого и последнего */
.breadcrumb-item:not(:first-child):not(:last-child) { 
  color: #666; 
}

/* 9. Селектор последнего элемента */
.breadcrumb-item:last-child { color: #000; }

/* 10. Селектор первого элемента */
.breadcrumb-item:first-child { color: #007bff; }

/* 11. Селектор n-го элемента */
.breadcrumb-item:nth-child(3) { color: #666; }

/* 12. Селектор нечетных элементов */
.breadcrumb-item:nth-child(odd) { /* стили */ }

/* 13. Селектор четных элементов */
.breadcrumb-item:nth-child(even) { /* стили */ }

/* 14. Селектор элементов начиная со второго */
.breadcrumb-item:nth-child(n+2) { margin-left: 5px; }

/* 15. Селектор элементов до третьего */
.breadcrumb-item:nth-child(-n+3) { /* стили */ }

/* 16. Селектор с псевдоэлементом */
.breadcrumb-link:hover::before { content: "👉 "; }

/* 17. Селектор по атрибуту с началом */
a[href^="/catalog"] { text-decoration: underline; }

/* 18. Селектор по атрибуту с содержанием */
a[href*="electronics"] { font-style: italic; }
```

## Пример 9: Пагинация

```html
<div class="pagination" data-page="3" data-total-pages="10">
  <a href="/page/1" class="page-link first" title="Первая страница">«</a>
  <a href="/page/2" class="page-link prev" title="Предыдущая страница">‹</a>
  
  <a href="/page/1" class="page-link">1</a>
  <a href="/page/2" class="page-link">2</a>
  <span class="page-link current" aria-current="page">3</span>
  <a href="/page/4" class="page-link">4</a>
  <a href="/page/5" class="page-link">5</a>
  
  <span class="page-ellipsis">…</span>
  
  <a href="/page/9" class="page-link">9</a>
  <a href="/page/10" class="page-link">10</a>
  
  <a href="/page/4" class="page-link next" title="Следующая страница">›</a>
  <a href="/page/10" class="page-link last" title="Последняя страница">»</a>
</div>
```

**CSS селекторы:**
```css
/* 1. Селектор по тегу и классу */
div.pagination { display: flex; }

/* 2. Селектор по атрибуту */
[data-page] { justify-content: center; }

/* 3. Селектор по атрибуту с значением */
[data-total-pages="10"] { /* стили */ }

/* 4. Селектор по классу */
.page-link { 
  padding: 8px 12px; 
  border: 1px solid #ddd; 
  margin: 0 2px; 
}

/* 5. Селектор по нескольким классам */
.page-link.current { 
  background: #007bff; 
  color: white; 
  border-color: #007bff; 
}

/* 6. Селектор по атрибуту */
[aria-current] { font-weight: bold; }

/* 7. Селектор по атрибуту title */
[title] { cursor: help; }

/* 8. Селектор по атрибуту с началом */
[title^="Первая"], 
[title^="Последняя"] { font-weight: bold; }

/* 9. Селектор по атрибуту с содержанием */
[title*="страница"] { padding: 8px; }

/* 10. Селектор всех кроме */
.page-link:not(.current) { 
  background: white; 
  color: #007bff; 
}

/* 11. Селектор по месту */
.page-link:first-of-type { border-radius: 4px 0 0 4px; }
.page-link:last-of-type { border-radius: 0 4px 4px 0; }

/* 12. Селектор по состоянию */
.page-link:hover:not(.current) { 
  background: #f8f9fa; 
  border-color: #dee2e6; 
}

/* 13. Селектор по состоянию focus */
.page-link:focus { 
  outline: 2px solid #4d90fe; 
  z-index: 1; 
}

/* 14. Селектор по n-му дочернему элементу */
.pagination > :nth-child(3) { margin-left: 10px; }
.pagination > :nth-last-child(3) { margin-right: 10px; }

/* 15. Селектор элементов с 4-го по 8-й */
.pagination > :nth-child(n+4):nth-child(-n+8) { /* стили */ }

/* 16. Селектор псевдоэлемента */
.page-ellipsis::before { content: "..."; }

/* 17. Селектор с отрицанием по атрибуту */
.page-link:not([href]) { cursor: default; }
```

## Пример 10: Древовидная структура

```html
<div class="tree-view" id="file-tree">
  <ul class="tree-root">
    <li class="tree-node" data-type="folder" data-expanded="true">
      <span class="tree-label">Документы</span>
      <ul class="tree-branch">
        <li class="tree-node" data-type="file" data-extension="pdf">
          <span class="tree-label">Отчет.pdf</span>
        </li>
        <li class="tree-node" data-type="folder" data-expanded="false">
          <span class="tree-label">Рабочие файлы</span>
          <ul class="tree-branch">
            <li class="tree-node" data-type="file" data-extension="docx">
              <span class="tree-label">Проект.docx</span>
            </li>
            <li class="tree-node" data-type="file" data-extension="xlsx">
              <span class="tree-label">Бюджет.xlsx</span>
            </li>
          </ul>
        </li>
      </ul>
    </li>
    <li class="tree-node" data-type="folder" data-expanded="true">
      <span class="tree-label">Изображения</span>
      <ul class="tree-branch">
        <li class="tree-node" data-type="file" data-extension="jpg">
          <span class="tree-label">Фото1.jpg</span>
        </li>
      </ul>
    </li>
  </ul>
</div>
```

**CSS селекторы:**
```css
/* 1. Селектор по ID */
#file-tree { font-family: monospace; }

/* 2. Селектор по тегу и классу */
ul.tree-root { padding-left: 0; }

/* 3. Селектор потомков */
.tree-view ul { padding-left: 20px; }

/* 4. Селектор по атрибуту */
[data-type] { list-style-type: none; }

/* 5. Селектор по значению атрибута */
[data-type="folder"] { font-weight: bold; }
[data-type="file"] { font-weight: normal; }

/* 6. Селектор по другому атрибуту */
[data-extension]::before { 
  content: "📄 "; 
  margin-right: 5px; 
}

/* 7. Селектор по значению другого атрибута */
[data-extension="pdf"]::before { content: "📕 "; }
[data-extension="docx"]::before { content: "📘 "; }
[data-extension="xlsx"]::before { content: "📗 "; }
[data-extension="jpg"]::before { content: "📷 "; }

/* 8. Селектор по нескольким атрибутам */
[data-type="folder"][data-expanded]::before { 
  content: "📂 "; 
}

/* 9. Селектор по значению второго атрибута */
[data-expanded="true"]::before { content: "📂 "; }
[data-expanded="false"]::before { content: "📁 "; }

/* 10. Селектор дочерних элементов */
.tree-node > .tree-label { cursor: pointer; }

/* 11. Селектор соседнего элемента */
.tree-label + .tree-branch { margin-top: 5px; }

/* 12. Селектор по состоянию */
.tree-label:hover { background: #f0f0f0; }

/* 13. Селектор с отрицанием */
.tree-node:not([data-expanded]) > .tree-branch { 
  display: none; 
}

/* 14. Селектор потомков с определенным состоянием */
.tree-node[data-expanded="true"] > .tree-branch { 
  display: block; 
}

/* 15. Селектор всех li кроме корневых */
.tree-root > li { border-left: 1px solid #ccc; }

/* 16. Селектор элементов второго уровня */
.tree-root > li > .tree-branch > li { 
  border-left-color: #999; 
}

/* 17. Селектор элементов третьего уровня */
.tree-root > li > .tree-branch > li > .tree-branch > li { 
  border-left-color: #666; 
}

/* 18. Селектор первого потомка */
.tree-branch > li:first-child { margin-top: 5px; }

/* 19. Селектор последнего потомка */
.tree-branch > li:last-child { margin-bottom: 5px; }

/* 20. Селектор единственного потомка */
.tree-branch > li:only-child { font-style: italic; }

/* 21. Селектор пустых веток */
.tree-branch:empty { display: none; }
```

## Классификация использованных селекторов:

### Базовые селекторы:
1. **По тегу**: `div`, `p`, `a`, `button`
2. **По классу**: `.class-name`
3. **По ID**: `#element-id`
4. **Универсальный**: `*`
5. **По атрибуту**: `[attribute]`, `[attribute=value]`

### Комбинаторы:
6. **Пробел (потомки)**: `div p`
7. **> (дети)**: `ul > li`
8. **+ (соседний)**: `h1 + p`
9. **~ (последующие)**: `h1 ~ p`

### Псевдоклассы:
10. **Динамические**: `:hover`, `:focus`, `:active`
11. **Структурные**: `:first-child`, `:last-child`, `:nth-child()`
12. **Состояния форм**: `:checked`, `:disabled`, `:required`
13. **Отрицание**: `:not()`
14. **Типа элемента**: `:first-of-type`, `:last-of-type`

### Псевдоэлементы:
15. `::before`, `::after`, `::placeholder`

### Селекторы атрибутов:
16. **Начинается с**: `[attribute^="value"]`
17. **Заканчивается на**: `[attribute$="value"]`
18. **Содержит**: `[attribute*="value"]`

